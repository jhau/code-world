# codebase-world

Compile codebases into stable, explorable 3D worlds that serve as living
documentation, co-authored by agents and humans.

Start with `CLAUDE.md` for project context, `spec/world-ir-v0.2.md` for the
IR specification, and `renderer/ROADMAP.md` for what's being built now.
